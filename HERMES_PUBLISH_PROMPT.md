# Hermes Publish Request

Please publish this static messaging app and return the live public URL.

## App Location

Static app folder:

```text
/Users/isaaccohen/Documents/Codex/2026-06-18/create-a-messaging-app/outputs/messaging-app
```

Ready-to-upload ZIP:

```text
/Users/isaaccohen/Documents/Codex/2026-06-18/create-a-messaging-app/outputs/messaging-app-publish.zip
```

## Publish Details

- This is a static HTML/CSS/JS app.
- No build command is required.
- Entry file: `index.html`.
- Assets are local under `assets/`.
- The app does not require a backend.
- It can be hosted on Netlify, Vercel, GitHub Pages, Cloudflare Pages, or any static web host.

## What To Do

1. Deploy the contents of the `messaging-app` folder, or upload `messaging-app-publish.zip`.
2. Make sure the deployed site serves `index.html` at the root URL.
3. Confirm that these files are included:
   - `index.html`
   - `styles.css`
   - `app.js`
   - `assets/lucide.min.js`
   - all `assets/avatar-*.jpg`
   - all `assets/attachment-*.svg`
4. Open the live URL and verify:
   - WhatsApp-style chat UI loads.
   - Profile photos load.
   - Icons load.
   - Sending a message works.
   - Attachment preview works.
   - New conversation dialog opens.
   - Mobile layout has no horizontal scroll.

## Return

Please return:

- Live public URL.
- Hosting provider used.
- Any deployment notes or changes made.

Do not change the product design unless required for deployment.
